<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - DoctorPod</title>
    <script src="/js/branding_config.js"></script>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        
        :root {
            --primary: #10b981;
            --danger: #ef4444;
            --warning: #f59e0b;
            --bg: #f8fafc;
            --surface: #ffffff;
            --text: #1e293b;
            --text-muted: #64748b;
            --border: #e2e8f0;
            --radius: 12px;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            color: var(--text);
        }
        
        .error-container {
            background: var(--surface);
            padding: 2.5rem;
            border-radius: var(--radius);
            box-shadow: 0 10px 40px rgba(0,0,0,0.1);
            max-width: 500px;
            width: 100%;
            text-align: center;
        }
        
        .error-icon {
            font-size: 4rem;
            margin-bottom: 1.5rem;
            opacity: 0.8;
        }
        
        .error-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--danger);
            margin-bottom: 1rem;
        }
        
        .error-message {
            font-size: 1rem;
            color: var(--text-muted);
            line-height: 1.6;
            margin-bottom: 2rem;
        }
        
        .patient-greeting {
            font-size: 0.875rem;
            color: var(--text-muted);
            margin-bottom: 2rem;
            font-style: italic;
        }
        
        .actions {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        
        .btn {
            padding: 0.875rem 1.5rem;
            border: none;
            border-radius: 8px;
            font-size: 0.875rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }
        
        .btn-primary {
            background: var(--primary);
            color: white;
        }
        
        .btn-primary:hover {
            background: #059669;
            transform: translateY(-1px);
        }
        
        .btn-secondary {
            background: var(--bg);
            color: var(--text);
            border: 1px solid var(--border);
        }
        
        .btn-secondary:hover {
            background: #f1f5f9;
        }
        
        .btn-contact {
            background: var(--warning);
            color: white;
        }
        
        .btn-contact:hover {
            background: #d97706;
        }
        
        .logo {
            max-height: 40px;
            margin-bottom: 1.5rem;
            opacity: 0.6;
        }
        
        @media (max-width: 480px) {
            .error-container {
                padding: 2rem 1.5rem;
            }
            
            .error-icon {
                font-size: 3rem;
            }
            
            .error-title {
                font-size: 1.25rem;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <img src="/asset/logo/doctor_logo.png" alt="DoctorPod" class="logo" id="errorLogo">
        
        <div class="error-icon">⚠️</div>
        
        <h1 class="error-title"><%= title %></h1>
        
        <% if (locals.patientName && patientName !== 'Patient') { %>
        <p class="patient-greeting">Hi <%= patientName %>,</p>
        <% } %>
        
        <p class="error-message"><%= message %></p>
        
        <div class="actions">
            <% if (locals.supportActions && supportActions.length > 0) { %>
                <% supportActions.forEach(action => { %>
                    <% if (action.action === 'contact') { %>
                        <button class="btn btn-contact" onclick="contactDoctor()">
                            📞 <%= action.text %>
                        </button>
                    <% } else if (action.action === 'retry') { %>
                        <button class="btn btn-primary" onclick="window.location.reload()">
                            🔄 <%= action.text %>
                        </button>
                    <% } else if (action.url) { %>
                        <a href="<%= action.url %>" class="btn btn-secondary">
                            <%= action.text %>
                        </a>
                    <% } %>
                <% }); %>
            <% } else { %>
                <a href="/" class="btn btn-primary">🏠 Go Home</a>
            <% } %>
        </div>
    </div>

    <script src="/js/brandingConfigLoader.js"></script>
    <script>
        function contactDoctor() {
            alert('Please contact your doctor directly or call the clinic for assistance with joining your video consultation.');
        }
        
        // Auto-refresh page every 30 seconds for time-sensitive errors
        if (window.location.search.includes('retry=auto')) {
            setTimeout(() => {
                window.location.reload();
            }, 30000);
        }
    </script>
</body>
</html>