# Environment Variables for DoctorPod

# Base URL for the application (REQUIRED for production)
# For local development: http://localhost:3000
# For Render deployment: https://doctorpod.onrender.com
BASE_URL=https://doctorpod.onrender.com

# PDF storage directory (relative to project root or absolute path)
PDF_DIR=public/pdfs/

# Database Configuration
# DATABASE_TYPE: 'sqlite' for local development, 'postgres' for production
DATABASE_TYPE=sqlite

# SQLite Configuration (for local development)
DB_PATH=./database/doctorpod.db

# PostgreSQL Configuration (for production on Render)
# Get this from your Render PostgreSQL instance
# Format: postgres://username:password@host:port/database
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Load Sample Data (set to 'true' to load sample data on database initialization)
# Use for development/testing only - DO NOT enable in production
# LOAD_SAMPLE_DATA=true

# Session Secret (change this in production)
SESSION_SECRET=your-secret-key-here

# Email Configuration (if using email notifications)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# Google Drive Configuration (for PDF backup on Render)
# Get credentials from: https://console.cloud.google.com/iam-admin/serviceaccounts
# Paste the entire JSON content as a single line (no line breaks)
# GOOGLE_DRIVE_CREDENTIALS={"type":"service_account","project_id":"..."}
# GOOGLE_DRIVE_FOLDER_ID=your-google-drive-folder-id
